//Usa un bucle forof para recorrer todos los destinos del array. Imprime en un ***console.log*** sus valores.

const placesToTravels = ['Japon', 'Venecia', 'Murcia', 'Santander', 'Filipinas', 'Madagascar'];
for (let i = 0; i < placesToTravels.length; i++) {
    const placesToTravel = placesToTravels[i];{
     console.log(placesToTravel, i);
    }
}
